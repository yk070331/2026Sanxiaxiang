# 红韵乔林自动化测试报告

## 1. 测试概况

- 测试日期：2026年8月24日
- 测试环境：Windows、本地Node.js运行时
- 测试性质：云函数与客户端同步逻辑自动化测试
- 真实云环境：未连接，数据库行为使用可记录的模拟对象

## 2. 被测模块

| 模块 | 文件 | 测试重点 |
| --- | --- | --- |
| 已审核内容读取 | `cloudfunctions/contentService/index.js` | 集合白名单、已发布内容读取 |
| 游客记录服务 | `cloudfunctions/visitorRecords/index.js` | 身份获取、重复打卡、非法参数、纠错内容校验 |
| 内容审核服务 | `cloudfunctions/contentAdmin/index.js` | 管理员角色、集合白名单、草稿防伪造、审核发布 |
| 离线同步层 | `miniprogram/utils/visitorSync.js` | 未配置云环境入队、重复任务去重、恢复后补传 |

## 3. 测试结果

| 编号 | 测试场景 | 预期 | 结果 |
| --- | --- | --- | --- |
| A01 | 重复提交同一路线同一点位打卡 | 返回原记录，不重复写入 | 通过 |
| A02 | 使用包含路径穿越字符的点位ID | 拒绝非法参数 | 通过 |
| A03 | 首次提交有效打卡 | 新增一条记录 | 通过 |
| A04 | 纠错说明少于5个字符 | 拒绝提交 | 通过 |
| A05 | 无微信身份读取打卡 | 返回未认证错误 | 通过 |
| A06 | 非管理员保存草稿 | 拒绝访问 | 通过 |
| A07 | 访问管理员角色集合 | 集合白名单拒绝 | 通过 |
| A08 | 客户端伪造发布状态和openid | 强制改为草稿并移除伪造字段 | 通过 |
| A09 | 录入员读取审核队列 | 拒绝访问 | 通过 |
| A10 | 审核员批准待审核资料 | 状态更新为published | 通过 |
| A11 | 内容读取服务访问非公开集合 | 集合白名单拒绝 | 通过 |
| A12 | 未配置云环境时打卡 | 进入本地待同步队列 | 通过 |
| A13 | 同一打卡重复离线入队 | 队列保持一条 | 通过 |
| A14 | 恢复云环境后刷新队列 | 成功补传并清空队列 | 通过 |
| A15 | 在线提交有效打卡 | 直接同步，不进入队列 | 通过 |

## 4. 执行结果

```text
cloudfunctions.test.js: all tests passed
visitorSync.test.js: all tests passed
```

新增及相关云函数JS/JSON静态语法检查结果：0个错误。

## 5. 尚需完成的测试

- 微信开发者工具编译与控制台检查。
- 微信云开发真实环境部署测试。
- Android和iOS真机兼容性测试。
- 定位允许、拒绝和系统关闭定位三种场景。
- 腾讯地图导航目标核对。
- 微信扫码、相册大图、关怀模式和弱网实测。
- 小程序体验码长期可访问性测试。

## 6. 结论

当前后端核心权限与游客打卡同步逻辑通过本地自动化测试，可以进入页面接入和真实云环境验证阶段。自动化测试不能替代真机和云端测试，最终参赛测试报告将在体验版发布后补充设备、截图和问题修复提交记录。

