<!--pages/village-info/index.wxml - 村情基础信息页-->
<view class="container">
  <!-- 村落名称 -->
  <view class="info-hero">
    <text class="info-village-name">🏘️ {{info.basic.name}}</text>
    <text class="info-village-sub">{{info.basic.administrative}}</text>
  </view>
  <view style="margin: 20rpx 28rpx 0; padding: 20rpx 24rpx; border-radius: 16rpx; background: #FFF3D6; border-left: 8rpx solid #D49A20;">
    <text style="display: block; color: #624500; font-size: 26rpx; font-weight: 600;">数据状态：调研台账·待村委复核</text>
    <text style="display: block; margin-top: 8rpx; color: #7A6541; font-size: 23rpx; line-height: 1.65;">
      除已标明来源的腾讯地图坐标外，人口、土地、收入、水质、设施和红色资源数量等须以村委或主管部门最终审核数据为准。
    </text>
  </view>

  <!-- 分类 Tab -->
  <scroll-view class="section-tabs" scroll-x="true" enable-flex>
    <view
      class="section-tab {{currentSection === section.id ? 'active' : ''}}"
      wx:for="{{sections}}"
      wx:key="id"
      wx:for-item="section"
      data-id="{{section.id}}"
      data-name="{{section.name}}"
      bindtap="onSectionChange"
    >
      <text class="section-tab-icon">{{section.icon}}</text>
      <text class="section-tab-name">{{section.name}}</text>
    </view>
  </scroll-view>

  <!-- 内容区域 -->
  <view class="info-content">
    <view class="info-card">
      <view class="info-card-header">
        <view class="label-dot"></view>
        <text class="info-card-title">{{currentSectionName}}</text>
      </view>

      <!-- 键值对表格 -->
      <view class="info-table">
        <view class="info-row" wx:for="{{rows}}" wx:key="label">
          <text class="info-row-label">{{item.label}}</text>
          <text class="info-row-value">{{item.value}}</text>
        </view>
      </view>

      <!-- 空状态 -->
      <view class="empty-state" wx:if="{{rows.length === 0}}">
        <text class="empty-text">暂无数据</text>
      </view>
    </view>
  </view>

  <!-- 资料维护入口对游客开放可见，但云函数会按 OPENID 校验管理员角色。 -->
  <view class="info-content">
    <view class="info-card">
      <view class="info-card-header">
        <view class="label-dot"></view>
        <text class="info-card-title">村级资料共建</text>
      </view>
      <text class="info-row-value">村级录入员、实践团队管理员和审核员可在此维护资料；普通游客无法写入或发布内容。</text>
      <button class="btn-primary" bindtap="onGoToAdmin">资料录入与审核</button>
    </view>
  </view>

  <view class="safe-area-bottom"></view>
</view>
